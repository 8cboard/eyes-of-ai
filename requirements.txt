# VisionTracker — requirements.txt
# Install with: pip install -r requirements.txt
#
# For CUDA GPU support, install torch separately first:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# For Apple Silicon (MPS):
#   pip install torch torchvision
# For CPU-only:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

# --- Core CV ---
opencv-python>=4.8.0
numpy>=1.24.0

# --- Tracking ---
supervision>=0.18.0        # ByteTrack wrapper (MIT)
scipy>=1.11.0              # Hungarian algorithm for centroid tracker fallback

# --- Detection: YOLO (optional but recommended) ---
ultralytics>=8.3.0         # YOLOv8 / YOLO11 — required for --detector yolo
                           # Models (yolo11n.pt etc.) are auto-downloaded on first run

# --- Identification: Remote LLM ---
requests>=2.31.0           # HTTP client for remote LLM server
urllib3>=2.0.0

# --- Optional: Background Removal (edge detector only) ---
rembg>=2.0.50              # Optional: for --use-bg-removal flag
onnxruntime>=1.15.0        # Required by rembg

# --- Utilities ---
python-dotenv>=1.0.0       # Optional: load REMOTE_URL from .env file

# --- Testing ---
pytest>=7.4.0
