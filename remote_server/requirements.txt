# VisionTracker Remote Server — requirements
#
# ── IMPORTANT: Install llama-cpp-python with prebuilt CUDA wheels ─────────────
# Do NOT use plain "pip install llama-cpp-python" — it will compile from source
# and may fail or produce a CPU-only binary.
#
# For CUDA 12.1 (Colab T4 / Kaggle P100 / most modern GPUs):
#   pip install llama-cpp-python \
#     --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu121
#
# Other CUDA versions:
#   cu122  → CUDA 12.2
#   cu124  → CUDA 12.4
#   cu125  → CUDA 12.5
#   cpu    → CPU-only (slow for inference)
#
# Full wheel list: https://abetlen.github.io/llama-cpp-python/whl/
# ─────────────────────────────────────────────────────────────────────────────

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Image processing
pillow>=10.0.0

# LLM inference
# llama-cpp-python — install SEPARATELY with CUDA wheels (see above)
# transformers + accelerate for HuggingFace safetensors models
transformers>=4.40.0
accelerate>=0.25.0
torch>=2.1.0

# Tunnel (for Colab / Kaggle public URL)
pyngrok>=7.0.0

# Utilities
numpy>=1.24.0
pydantic>=2.5.0
