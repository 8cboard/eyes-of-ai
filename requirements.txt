# VisionTracker — requirements.txt
# Install with: pip install -r requirements.txt
#
# For CUDA GPU support, install torch separately first:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# For Apple Silicon (MPS):
#   pip install torch torchvision
# For CPU-only:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

# --- Core CV ---
opencv-python>=4.8.0
numpy>=1.24.0

# --- Object Detection ---
ultralytics>=8.0.200       # YOLOv8 family (AGPL-3.0 license)

# --- Tracking ---
supervision>=0.18.0        # ByteTrack wrapper (MIT)
scipy>=1.11.0              # Hungarian algorithm for centroid tracker fallback

# --- Identification: OpenRouter API ---
requests>=2.31.0           # HTTP client for OpenRouter
urllib3>=2.0.0

# --- Utilities ---
python-dotenv>=1.0.0       # Optional: load OPENROUTER_API_KEY from .env file

# --- Testing ---
pytest>=7.4.0
